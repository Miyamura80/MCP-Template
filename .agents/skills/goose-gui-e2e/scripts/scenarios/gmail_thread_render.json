{
  "comment": "dom_contains uses thread content present in BOTH the reader view (race won) and the curated-inbox fallback the app shows when it misses the thread result (race lost); GMAIL_FAKE_BACKEND serves both paths, so the assertion is deterministic.",
  "prompt": "Open the Series A term sheet email thread.",
  "plan": [
    {
      "match": "gmail_get_thread",
      "args": {
        "thread_id": "t-1001"
      }
    }
  ],
  "final": "Opened the thread.",
  "app_uri": "ui://mymcp/gmail_inbox",
  "expect": {
    "tool_called": "gmail_get_thread",
    "round_trip": true,
    "app_rendered": true,
    "dom_contains": [
      "Series A term sheet",
      "Dana Whitfield"
    ]
  }
}
