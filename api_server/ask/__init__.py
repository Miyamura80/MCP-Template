"""Ask package: BM25 docs retrieval + LLM answer synthesis.

This is an ``api_server``-only feature. It is intentionally NOT a registry
service, CLI command, or MCP tool - the only transport is the ``/ask`` route.
"""
