{
  "name": "landing-page",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "start": "sirv dist --host 0.0.0.0 --port ${PORT:-8080} --single --dotfiles --quiet",
    "astro": "astro"
  },
  "dependencies": {
    "astro": "^5.6.1",
    "sirv-cli": "^3.0.0"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.1.5",
    "tailwindcss": "^4.1.5"
  }
}
